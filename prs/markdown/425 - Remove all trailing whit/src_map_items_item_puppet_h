@@ -44,10 +44,10 @@ class CItemPuppet : public CItem
     void   setEquipSlot(uint32 slot);
     uint32 getElementSlots();
     void   setElementSlots(uint32 slots);
-	
+
 private:
     uint8  m_equipSlot;
     uint32 m_elementSlots;
 };
 
-#endif
\ No newline at end of file
+#endif