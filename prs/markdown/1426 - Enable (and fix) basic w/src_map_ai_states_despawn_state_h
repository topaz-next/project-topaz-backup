@@ -34,8 +34,6 @@ class CDespawnState : public CState
     virtual bool CanChangeState() override;
     virtual bool CanFollowPath() override { return false; }
     virtual bool CanInterrupt() override { return false; }
-private:
-    duration m_spawnTime;
 };
 
 #endif